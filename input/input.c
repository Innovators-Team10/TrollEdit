// this is sample
// program
struct p {
	int n;
} P;

int main() {
	switch(hroch) {
		case slon : add(foo,9);
		if (abc) d = 9;
		case jelen : break;
		default: break;
	}
  int foo = 8, bar=9;
  while   ( foo > 4 ) {
    printf("Result is: %d and %d\n", 
add(foo,add(0,1)+7), n);
    foo = foo - 1;
  }
  return 0;
  /*
  vratila sa
		nula
  */
  //bla
} 
 hfloat d = 7.56; // d=    8
#include <stdio.h>
int  c=0;

int add(int a, int b) {
  int  c=0;
  return a + b;
}
int n = add(7,1);
abc